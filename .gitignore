# Action deps are installed in CI (npm install), never committed.
node_modules/
package-lock.json

# OS / editor cruft
.DS_Store

# Wrangler local cache / state (apps/api)
.wrangler/

# Signing keys / local secrets (never commit)
.secrets/
