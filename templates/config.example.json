{
  "_comment": "Rename to config.json to activate. `preview` builds and serves the pushed code in CI so screenshots always match the commit — no deployed URL needed. `routes` is an optional hint list that helps Gemini pick which pages to screenshot.",
  "preview": {
    "command": "npm ci && npm run build && npx serve -l 4173 dist",
    "port": 4173,
    "readyPath": "/",
    "timeoutSeconds": 180
  },
  "routes": ["/", "/example", "/example/nested"]
}
